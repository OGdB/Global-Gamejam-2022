using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// The Events manager causes events causing (mis)fortune for either side, giving the other more fortune and possibly changing the tides.
/// </summary>
public class EventsManager : MonoBehaviour
{
    [SerializeField] private SideManager lightSideManager;
    [SerializeField] private SideManager darkSideManager;


}
